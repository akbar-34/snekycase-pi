// import React from 'react';
// import { FONTS } from './path-to-your-fonts';

// const FontSelector = ({ selectedFont, onFontChange }) => {
//   return (
//     <select value={selectedFont} onChange={onFontChange}>
//       {FONTS.map((font) => (
//         <option key={font.value} value={font.value}>
//           {font.label} {/* Make sure to render the label here */}
//         </option>
//       ))}
//     </select>
//   );
// };

// export default FontSelector;